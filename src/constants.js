'use strict';

export const MEASURE_WIDTH = 400;
export const REST_PERCENTAGE = 0.05;

export const STATUS = {
  STARTED: 'STARTED',
  SCORE_SCREEN: 'SCORE_SCREEN',
  FINISHED: 'FINISHED',
};

export const DEFAULT_TEMPO = 60;
